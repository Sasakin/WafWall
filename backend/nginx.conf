server {
    listen 80;
    default_type application/json;

    location / {
        return 200 '{"status":"ok","path":"$uri"}';
    }

    location = /health {
        return 200 '{"status":"UP"}';
    }
}